# Build output
bin/

# Local state (data_root)
.harness/
*.db
*.db-wal
*.db-shm
coverage.out

# Secrets: credentials, tokens, keys. .env.example is the tracked template.
.env
.env.*
!.env.example
.envrc
*.pem
*.key
*.crt
*.p12
*.pfx
id_rsa*
id_ed25519*
*.kubeconfig
kubeconfig
credentials
credentials.json
*.credentials
secrets.yaml
secrets.yml
*.secret.yaml
*.secret.yml

# The operator's own config and ad-hoc task specs. harness.example.yaml is the
# tracked template (README: copy it to harness.yaml); a tracked harness.yaml
# would put one machine's data_root and worker mode in everyone's checkout.
/harness.yaml
/task.json

# Local overrides of checked-in config
*.local.yaml
*.local.yml
*.local.json
.claude/settings.local.json
