# dateparse

Python 3.11 date helpers. Standard library only.

## Conventions
- Run `python3 -m unittest discover -s tests` before finishing. It must pass.
- Type hints on every public function.
- Never loosen a test to make it pass.
