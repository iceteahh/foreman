# slugger

Node 20 module, no dependencies. Tests run with the built-in runner.

## Conventions
- Run `node --test` before finishing. It must pass.
- CommonJS only; this package is required by older services.
- Never widen a regular expression to make a test pass.
