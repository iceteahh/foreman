apiVersion: v2
name: harness
description: Agent harness on claude -p — stateless orchestrator replicas, workers as Kubernetes Jobs
type: application
version: 0.5.0
appVersion: "0.5.0"
keywords: [claude, agents, harness]
